<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kraftwerk28.pocketcms">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.DBActivity">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />

                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />

                <data
                    android:host="pocket-cms.org"
                    android:scheme="https" />
                <data
                    android:host="pocket-cms.org"
                    android:scheme="http" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.LogoActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <activity android:name=".playground.RecyclerActivity" />
        <activity android:name=".playground.ChargerMeter" />
        <activity android:name=".MainActivity" />
        <activity android:name=".playground.GreetingActivity" />
        <activity android:name=".playground.FragmentActivity" />
        <activity
            android:name=".playground.CookieActivity"
            android:screenOrientation="sensor" />

        <service
            android:name=".playground.SampleService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".playground.MyIntentService"
            android:exported="false" />
    </application>

</manifest>
